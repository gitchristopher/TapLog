using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using TapLog.Domain.Entities;

namespace TapLog.Infrastructure.Persistence.Configurations
{
    //public class CycleConfiguration : IEntityTypeConfiguration<Cycle>
    //{
    //    public void Configure(EntityTypeBuilder<Cycle> builder)
    //    {
    //        //builder.Property(c => c.Name).HasMaxLength(32).IsRequired();
    //        //builder.Property(c => c.IsCurrent).IsRequired();

    //        //builder.HasMany(y => y.TestCycles).WithOne(c => c.Cycle).IsRequired();
    //        //builder.HasMany(y => y.CycleStages).WithOne(c => c.Cycle).IsRequired();
    //    }
    //}
}
